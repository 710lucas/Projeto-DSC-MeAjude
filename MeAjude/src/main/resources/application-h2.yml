
spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:./h2testdb
    username: h2
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate.format_sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8081

# server:
#   servlet:
#     context-path: /api
# Diz ao spring que coloque /api antes de qualquer URL.
# Ou seja, se você quiser utilizar a rota /api/products, precisará indicar no controlador /products => /api vai ser adicionado automaticamente
